<!DOCTYPE html>
<html>
  <head>
    <title>Paynow</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link href="https://fonts.googleapis.com/css2?family=Monoton&display=swap" rel="stylesheet">


    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark ps-3">
    <%= link_to 'PAYNOW', root_path,  class:"logo" %>
      <div class="collapse navbar-collapse">
       <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
         <% if  user_signed_in?%>
        <% if  current_user && current_user.company %>
        <li class="nav-item active">
          <%= link_to 'Dados da Empresa', user_company_path(current_user.company.token), class:"nav-link" %>
        </li>
        <li class="nav-item active">
          <%= link_to 'Meios de Pagamento Disponiveis', user_payment_methods_path, class:"nav-link" %>
        </li>

        <li class="nav-item active">
          <%= link_to 'Meios de Pagamento Escolhidos', user_company_chosen_payments_path(current_user.company_id), class:"nav-link" %>
        </li>

        <% else %>
        <li class="nav-item active">
          <%= link_to 'Cadastrar Empresa', new_user_company_path, class:"nav-link" %>
        </li>
        <% end %>
        <li class="nav-item">
           <span class="text-light nav-link disabled"><%= current_user.email %></span>
        </li>
         <%= link_to 'Sair',  destroy_user_session_path, method: :delete, class:"nav-link" %>
         <% else %>
        </li>
          <li class="nav-item active">
          <%= link_to 'Logar', new_user_session_path, class:"nav-link" %>

        <li class="nav-item active">
          <%= link_to 'Cadastre-se', new_user_registration_path, class:"nav-link" %>
        </li>
       <% end %>
      </li>
    </ul>
  </div>
</nav>
  </header>
      <% flash.each do |nome, mensagem| %>
      <%= content_tag :div, mensagem, class: 'base-alert ' + nome %>
    <% end %>
      <div class="container-fluid">
        <%= yield %>
      </div>
        </div>
            </div>
<footer class="bg-dark text-center text-white">
  <!-- Grid container -->
  <div class="container p-4 pb-0">
    <!-- Section: Social media -->
    <section class="mb-4">
      <!-- Facebook -->
      <a class="btn btn-outline-light btn-floating m-1 " href="#!" role="button">Facebook</a>
      <!-- Instagram -->
      <a class="btn btn-outline-light btn-floating m-1" href="#!" role="button">Instagram</a>
      <!-- Twitter -->
      <a class="btn btn-outline-light btn-floating m-1" href="#!" role="button" >Twitter</a>
    </section>
    <!-- Section: Social media -->
  </div>
  <!-- Grid container -->

  <!-- Copyright -->
  <div class="text-center p-3" style="background-color: rgba(0, 0, 0, 0.2);">
    © 2021 Copyright:
    <a class="text-white" >Paynow</a>
  </div>
  <!-- Copyright -->
</footer>
  </body>
</html>
